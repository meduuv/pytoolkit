[build-system]
requires=["setuptools>=69"]
build-backend="setuptools.build_meta"
[project]
name="pytoolkit"
version="0.1.0"
description="Practical Python utilities"
requires-python=">=3.10"
[tool.setuptools.packages.find]
where=["src"]
